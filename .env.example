# Backend Environment Variables for Render

# Database Configuration (PostgreSQL)
DATABASE_URL=postgresql://user:password@localhost:5432/notion_db
DATABASE_USER=postgres
DATABASE_PASSWORD=your_secure_password_here

# JPA/Hibernate Configuration
JPA_DDL_AUTO=validate
JPA_SHOW_SQL=false
JPA_FORMAT_SQL=false

# Security - JWT Configuration
# IMPORTANT: Generate a secure key in production (min 32 characters)
JWT_SECRET=your-secret-key-must-be-at-least-32-characters-long-replace-this-in-production

# CORS Configuration
# Add your frontend URL here
CORS_ALLOWED_ORIGINS=http://localhost:4200,https://notion-template-indol.vercel.app

# JWT Token Expiration
JWT_EXPIRATION_HOURS=2

# Server Port (Render uses PORT environment variable)
PORT=8080
